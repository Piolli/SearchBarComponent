# Uncomment the next line to define a global platform for your project
# platform :ios, '9.0'

target 'SearchBarComponent' do
  # Comment the next line if you don't want to use dynamic frameworks
  use_frameworks!

  # Pods for SearchBarComponent
  pod 'Alamofire', '~> 4.9'
  pod 'SwiftyJSON', '~> 4.0'

  target 'SearchBarComponentTests' do
    inherit! :search_paths
    # Pods for testing
  end

end
